<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the export scratch directory is ever shared out, and only for
         PSBTs, wallet descriptors and debug-log bundles the user explicitly
         asked to export. The datadir itself is never exposed. -->
    <files-path name="exports" path="exports/" />
    <cache-path name="share_cache" path="share/" />
</paths>
